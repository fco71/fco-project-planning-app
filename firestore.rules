rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
      match /pins/{pinId} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
      match /globalNotes/{noteId} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
      match /nodes/{nodeId} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
      match /crossRefs/{refId} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
    }
  }
}
